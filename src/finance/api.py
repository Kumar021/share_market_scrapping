from rest_framework import routers
from trading import api_views as myapp_views

router = routers.DefaultRouter()


# router.register(r'share-name', myapp_views.ShareNameViewset),
# router.register(r'share-data', myapp_views.ShareMarketViewset),
